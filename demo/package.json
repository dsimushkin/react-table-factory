{
  "name": "react-table-factory-demo",
  "version": "0.1.0",
  "homepage": "http://dsimushkin.github.io/react-table-factory/",
  "private": true,
  "dependencies": {
    "node-sass": "^4.13.0",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "^3.2.0",
    "react-table-factory": "^0.2.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "rmlink": "yarn unlink react-table-factory && yarn install --force && yarn upgrade",
    "predeploy": "yarn rmlink && yarn build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "typescript": "^3.5.3"
  }
}
